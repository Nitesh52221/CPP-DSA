// Problem : misc. operators

#include <iostream>
using namespace std;
int main()
{
    // write your code here
    int x = 10;
    // sizeof
    cout << sizeof(x) << endl; // 4

    // ? :
    int y = 5;
    int res = (x > y) ? 1 : 0;
    cout << (res) << endl;

    // ,
    int a = (1, 3, 4, 5);
    cout << a << endl;

    // .(dot) and --> (arrow)

    // cast

    // & address-of

    // * pointer operator
}